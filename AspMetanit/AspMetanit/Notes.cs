namespace AspMetanitNotes
{
    public class Notes
    {
        /* минимальное API
         * var builder = WebApplication.CreateBuilder(args); //специальный класс строитель
         * var app = builder.Build(); //создание обьекта WebApplication
         * app.MapGet("/", () => "Hello World!"); // с помощью WebApplication можно настроить всю инфраструктуру
         * app.Run(); //необходим запуск приложения
         */


        /* await app.StartAsync();  
         * await Task.Delay(10000); // ждать 10 секунд
         * await app.StopAsync();
         */


    }
}
